SEG1 	SEGMENT
	OPER1 DB 5
	OPER2 DB 3 
SEG1 	ENDS

SEG2 	SEGMENT 
	RESULT DB ?
SEG2 	ENDS

CODE 	SEGMENT
ASSUME CS:CODE, DS:SEG1 
START:	 	
	MOV AX,SEG1
	MOV DS,AX
	MOV AH,OPER1
	ADD AH,OPER2
	ASSUME DS:NOTHING, DS:SEG2
	MOV AX,SEG2
	MOV DS,AX
	MOV RESULT,AH
	MOV AH,4CH
	INT 21H
CODE ENDS
END START
